# Speech-to-text Svelte app

A simple project to demonstrate speech-to-text functionality. Only tested on Chrome.

## Get started

Install dependencies:

```bash
npm i
```

Run in dev mode:

```bash
npm run dev
```

Check `package.json` for more commands.
